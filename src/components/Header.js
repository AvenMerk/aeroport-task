import React from 'react';

const Header = () => (
    <React.Fragment>
      <header className="aero-header">
          <h1>Расписание рейсов</h1>
      </header>
    </React.Fragment>
);

export default Header;